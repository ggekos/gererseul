{% extends 'base.html.twig' %}

{% block title %}GérerSeul : {{ product.name }}{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-sm-4">
            <div class="panel panel-success">
                <div class="panel-heading"><a href="{{ path('product', {'id': product.id}) }}">{{ product.name }}</a></div>
                <div class="panel-footer">{{ product.price|number_format(2, '.', ',') }}</div>
                <div class="panel-footer">
                    <form method='POST' action='{{ path('basketAdd', {'id': product.id}) }}'>
                        <div class="form-group row">
                            <label for="example-number-input" class="col-2 col-form-label">Quantity</label>
                            <div class="col-4">
                                <input class="form-control" type="number" value="" id="quantity" name="quantity">
                            </div>
                            <button type="submit" class="btn btn-primary">Add to basket</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}